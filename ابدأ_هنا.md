# ابدأ هنا — مشروع غياب زيد

نسخة محلية **قابلة للتعديل** و**جاهزة للنشر** من نظام الحضور «غيابي».

المسار على هذا الجهاز:

```text
/home/ubuntu/Desktop/غياب زيد
```

---

## ماذا يوجد في المجلد؟

| ملف | لمن؟ |
|-----|------|
| **`AGENTS.md`** | أي وكيل ذكاء اصطناعي — اقرأه أولاً عند فتح المشروع في Cursor/Claude/… |
| **`docs/TECHNICAL.md`** | تقنيات + مفاتيح + قاعدة البيانات |
| **`DEPLOYMENT.md`** | خطوات النشر على Netlify وSupabase |
| **`README.md`** | نظرة عامة |
| **`artifacts/ghiyabi/`** | كود التطبيق |

---

## تشغيل محلي (5 دقائق)

### المتطلبات

- Node.js **22** (أو 20+)
- تفعيل pnpm: `corepack enable`

### الخطوات

```bash
cd "/home/ubuntu/Desktop/غياب زيد"

pnpm install

cp artifacts/ghiyabi/.env.example artifacts/ghiyabi/.env
```

افتح `artifacts/ghiyabi/.env` وضع من لوحة Supabase (Settings → API):

```env
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_KEY
```

ثم:

```bash
pnpm --filter @workspace/ghiyabi dev
```

المتصفح: **http://localhost:5173**

---

## قاعدة البيانات (مرة واحدة لكل مشروع Supabase)

1. افتح Supabase → SQL Editor  
2. نفّذ بالترتيب:
   - `artifacts/ghiyabi/supabase/schema.sql`
   - `artifacts/ghiyabi/supabase/seed.sql`
3. أنشئ مستخدم Auth للمدير وأضف بريده في جدول `admins`.

تفاصيل الجداول والمفاتيح: **`docs/TECHNICAL.md`**.

---

## النشر على Netlify

1. اربط المستودع (أو ارفع هذا المجلد إلى GitHub).  
2. تأكد أن البناء من **جذر المجلد** (حيث `netlify.toml`).  
3. أضف متغيرات البيئة: `VITE_SUPABASE_URL` و `VITE_SUPABASE_ANON_KEY`.  
4. Publish directory يُقرأ تلقائياً: `artifacts/ghiyabi/dist/public`.

من الطرفية (بعد `netlify login`):

```bash
pnpm run deploy:production
```

---

## العمل مع وكيل (Cursor / غيره)

1. افتح المجلد `غياب زيد` كـ Workspace.  
2. اطلب من الوكيل: **«اقرأ AGENTS.md ثم docs/TECHNICAL.md»**.  
3. صِغ المهمة بوضوح (مثال: «أضف عمود ملاحظة في شاشة الحضور»).

---

## ملاحظات أمان

- لا ترفع ملف `.env` إلى GitHub.  
- لا تضع `SERVICE_ROLE` في الواجهة.  
- غيّر كلمات مرور الاختبار قبل الإنتاج.

---

بالتوفيق — مدرسة زيد بن ثابت 🌿

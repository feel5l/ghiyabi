{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "preinstall": "node -e \"try{require('fs').unlinkSync('package-lock.json')}catch(e){} try{require('fs').unlinkSync('yarn.lock')}catch(e){} if(!process.env.npm_config_user_agent?.startsWith('pnpm/')){console.error('Use pnpm instead');process.exit(1)}\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "deploy:production": "pnpm --filter @workspace/ghiyabi run build && pnpm exec netlify deploy --filter @workspace/ghiyabi --prod --no-build --dir=artifacts/ghiyabi/dist/public"
  },
  "private": true,
  "devDependencies": {
    "netlify-cli": "^26.2.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
